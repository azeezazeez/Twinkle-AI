# Twinkle AI — Fully Merged & Build-Fixed

Merged the requested Profile, Settings, sidebar expansion, OAuth, Charon Live Talk, and Live Talk chat-saving features into the uploaded Twinkle AI project.

## Local
Frontend: http://localhost:5173
Backend: http://localhost:8080
API: http://localhost:8080/api
Live Talk voice: Charon

## Run
Backend: `mvnw.cmd spring-boot:run`
Frontend: `npm install` then `npm run dev`

The existing application.properties was preserved and OAuth/local-session properties were added.
